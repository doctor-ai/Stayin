const style = theme => ({
  container:{
    display:"flex",
    minHeight:"90vh",
    justifyContent:"Center",
    alignItems:"center",
  },
  textField:{
    marginTop:theme.spacing(2)
   },
   button:{
     marginTop:theme.spacing(2),
   }
  });

  export default style;
